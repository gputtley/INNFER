# Train classifier models
- step: "TrainClassifier"
  submit: "configs/submit/condor_4_cpus.yaml"
  run_options: 
    disable_tqdm: True

# Evaluate classifier models
- step: "EvaluateClassifier"
  submit: "configs/submit/condor_4_cpus.yaml"

# Plot classifier models
- step: "PlotClassifier"
  submit: "configs/submit/condor_2_cpus.yaml"